name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke-a1_1p1:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install package with test dependencies
        run: pip install -e ".[dev]"

      - name: Compile C benchmark artifacts
        run: |
          cd contrib
          gcc -O2 -shared -fPIC -o sor_kernels.so sor_kernels.c
          gcc -O2 -o sor_benchmark sor_benchmark.c sor_kernels.so -Wl,-rpath,'$ORIGIN'

      - name: Smoke test (quick import check)
        run: python -c "from scicomp3 import solve_ivp, wave1d_rhs; print('Import OK')"

      - name: Run pytest
        run: pytest tests/ -v
